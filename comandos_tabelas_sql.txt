CREATE TABLE BOOTCAMP_USERS (
USER_ID INTEGER NOT NULL PRIMARY KEY,
USERNAME VARCHAR2(50) NOT NULL,
USER_PASSWORD VARCHAR2(24) NOT NULL,
USER_TYPE VARCHAR2(10) NOT NULL);

INSERT INTO BOOTCAMP_USERS (USER_ID, USERNAME, USER_PASSWORD, USER_TYPE)
    VALUES(1, "admin", "123", "admin");

INSERT INTO BOOTCAMP_USERS (USER_ID, USERNAME, USER_PASSWORD, USER_TYPE)
    VALUES(2, "common", "123", "common");

CREATE TABLE BOOTCAMP_TEACHERS (
TEACHER_ID INTEGER NOT NULL PRIMARY KEY,
TEACHER_NAME VARCHAR2(50) NOT NULL,
SUBJECT VARCHAR2(50) NOT NULL,
TEACHER_COST FLOAT NOT NULL,
EMAIL VARCHAR2(50) NOT NULL,
PHONE VARCHAR2(50) NOT NULL);

CREATE SEQUENCE TEACHER_ID_SEQ START WITH 1 INCREMENT BY 1;